# FastAPI
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Auth & DB
python-jose[cryptography]==3.3.0
bcrypt>=4.1.2,<5
python-multipart==0.0.17

# Database
sqlalchemy==2.0.36

# Firebase Admin (for OAuth sync)
firebase-admin==6.6.0

# Stripe
stripe==11.3.0

# Azure Blob Storage
azure-storage-blob==12.19.0

# AI — Three-Tier System (Claude + DeepSeek + Ollama + HF)
anthropic==0.39.0
openai==1.54.4
httpx>=0.27.0
huggingface-hub>=0.24.0
# chromadb and sentence-transformers excluded (too large for serverless, use API-based embeddings)

# Env
python-dotenv==1.0.1
pydantic-settings==2.6.1
alembic>=1.13.0
sentry-sdk[fastapi,sqlalchemy]>=2.8.0
stripe>=11.1.0
sendgrid>=6.11.0
pyotp>=2.9.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.24.0
